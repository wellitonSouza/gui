import alt from '../../../alt';
import DeviceHandlerStore from './DeviceHandlerStore';

const DeviceFormStore = alt.createStore(DeviceHandlerStore, 'DeviceFormStore');

export default DeviceFormStore;
